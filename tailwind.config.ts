import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#2563eb", // blue
        secondary: "#9333ea", // purple
        accent: "#f59e0b", // amber
      },
    },
  },
  plugins: [],
};
export default config;
